<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.camera.any" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Final_Project">

        <activity
            android:name=".views.activity.SignUp"
            android:exported="false" />
        <activity
            android:name=".views.activity.Login"
            android:exported="false" />
        <activity android:name=".views.activity.CameraActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity
            android:name=".views.activity.HomeMenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".MainActivity" android:exported="true" />

        <!--  Add-ons  -->
        <activity android:name=".views.activity.CreateMenuActivity" />
        <activity android:name=".views.activity.CreateRecipeActivity" />
        <activity android:name=".views.activity.RecipeListActivity" />
        <activity android:name=".views.activity.RecipeDetailActivity" />
        <activity android:name=".views.activity.MealDetailActivity" />
        <activity android:name=".views.activity.FoodResultActivity" />

        <!-- 🔹 Thêm dòng này -->
        <activity android:name=".views.activity.FridgeInventoryActivity" />
        <activity android:name=".views.activity.SearchResultsActivity" />
        <activity android:name=".views.activity.StartedScreen" />
    </application>


</manifest>